# Mock databases have been replaced by SQLAlchemy SQLite DB.
# This file is kept temporarily for backward compatibility if any imports were missed.
# All data operations should now use the database session.
users_db = []
appointments_db = []
otp_db = {}
